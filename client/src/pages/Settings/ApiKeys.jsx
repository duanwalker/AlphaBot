function ApiKeys() {
  return (
    <section>
      <h3>API Keys</h3>
      <p className="settings-copy">API key management will appear here. Rotate, revoke, and scope integration keys safely.</p>
    </section>
  );
}

export default ApiKeys;
